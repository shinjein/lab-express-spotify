<div class="col-md-4">
    <div class="tracklist">
        <div class="track-description">
            <h5 class="track-title">{{name}}</h5>
            <audio controls src="{{preview_url}}">
                Your browser does not support the <code>audio</code> element.
            </audio>
        </div>
    </div>
</div>