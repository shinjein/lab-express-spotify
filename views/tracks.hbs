<div class="container">
    <h1>Track List of {{name}} Search Result</h1>
    <div class="row">
        {{#each tracks}}
        {{> eachTrack}}
        {{/each}}
    </div>
    <a href="/">Home</a>
</div>